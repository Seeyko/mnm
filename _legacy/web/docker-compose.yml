services:
  mnm:
    build: .
    ports:
      - "3000:3000"
    environment:
      - MNM_REPO_ROOT=/repo
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
    volumes:
      - ${MNM_REPO_PATH:-.}:/repo:ro
      - mnm-data:/repo/.mnm

volumes:
  mnm-data:
